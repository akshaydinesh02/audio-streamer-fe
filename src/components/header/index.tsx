import { Button } from "../ui/button";

const Header = () => {
  return (
    <div className="w-full flex flex-col justify-center">
      <p>HEADER</p>
      <Button>Login</Button>
    </div>
  );
};

export default Header;
